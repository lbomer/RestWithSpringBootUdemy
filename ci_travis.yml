language: java
jdk:
  - oraclejdk11
services: docker
before_install:
  - sudo apt-get update
  - cd 16\ RestWithSpringBootUdemy\ Deploy\ in\ AWS/RestWithSpringBootUdemy/
  - echo "Let's strat Maven Package!"
  - mvn clean package
  - cd ..
  - echo "We are in the Docker folder!"
script:
  - echo "OK Here we GO!"
  - docker-compose build
before_deploy:
  - echo "${DOCKER_PASSWORD}" | docker login --username "${DOCKER_USERNAME}" --password-stdin
deploy:
  provider: script
  script:
    docker push bomerdosanjos/mysql-rest-with-spring-boot-udemy;
    docker push bomerdosanjos/rest-with-spring-boot-udemy;
  on:
    branch: master
notifications:
  email: false
